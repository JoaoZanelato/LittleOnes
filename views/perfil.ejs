<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Editar Perfil</title>
    <link rel="stylesheet" href="/stylesheets/Dashboard.css">
</head>
<body>
<div class="container">
    <header>
        <div class="header-content">
            <h1>Perfil de <%= usuario.nome %></h1>
        </div>
         <div class="header-actions">
            <a href="/dashboard-utilizador" class="btn-secondary">Voltar ao Dashboard</a>
        </div>
    </header>

    <div class="dwarves-section" style="background-color: #c6b8a3; color: #3d2b1f;">
        <form action="/perfil" method="POST" style="max-width: 600px; margin: auto;">
            <label>Nome:</label><input type="text" name="nome" value="<%= anao.nome %>" required>
            <label>Descrição:</label><textarea name="descricao"><%= anao.descricao %></textarea>
            <label>Idade:</label><input type="number" name="idade" value="<%= anao.idade %>">
            <label>Altura:</label><input type="number" step="0.01" name="altura" value="<%= anao.altura %>">
            <label>Peso:</label><input type="number" step="0.1" name="peso" value="<%= anao.peso %>">
            <label>URL da Foto:</label><input type="text" name="url_foto" value="<%= anao['url-foto'] %>">
            <label>Status:</label>
            <select name="status">
                <option value="ativo" <%= anao.status === 'ativo' ? 'selected' : '' %>>Ativo</option>
                <option value="inativo" <%= anao.status === 'inativo' ? 'selected' : '' %>>Inativo</option>
            </select>
            <button type="submit" class="btn-primary">Salvar Alterações</button>
        </form>
    </div>
</div>
</body>
</html>