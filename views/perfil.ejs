<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Perfil</title>
    <link rel="stylesheet" href="/stylesheets/main.css">
</head>
<body>
<div class="container">
    <header>
        <div class="header-content">
            <h1>Perfil de <%= usuario.nome %></h1>
        </div>
         <div class="header-actions">
            <a href="/dashboard-utilizador" class="btn btn-secondary">Voltar ao Dashboard</a>
        </div>
    </header>

    <div class="form-container" style="max-width: 700px; margin: auto;">
        <h2>Suas Informações</h2>
        <form action="/perfil" method="POST">
            <div>
                <label for="nome">Nome:</label>
                <input type="text" id="nome" name="nome" value="<%= anao.nome %>" required>
            </div>
            <div>
                <label for="descricao">Descrição:</label>
                <textarea id="descricao" name="descricao" rows="4"><%= anao.descricao || '' %></textarea>
            </div>
            <div>
                <label for="idade">Idade:</label>
                <input type="number" id="idade" name="idade" value="<%= anao.idade || '' %>">
            </div>
            <div>
                <label for="altura">Altura (metros):</label>
                <input type="number" id="altura" step="0.01" name="altura" value="<%= anao.altura || '' %>">
            </div>
            <div>
                <label for="peso">Peso (kg):</label>
                <input type="number" id="peso" step="0.1" name="peso" value="<%= anao.peso || '' %>">
            </div>
            <div>
                <label for="url_foto">URL da Foto:</label>
                <input type="text" id="url_foto" name="url_foto" value="<%= anao['url-foto'] || '' %>">
            </div>
            <div>
                <label for="status">Status:</label>
                <select id="status" name="status">
                    <option value="ativo" <%= anao.status === 'ativo' ? 'selected' : '' %>>Ativo (Visível para aluguel)</option>
                    <option value="inativo" <%= anao.status === 'inativo' ? 'selected' : '' %>>Inativo (Não visível para aluguel)</option>
                </select>
            </div>
            
            <button type="submit" class="btn btn-primary" style="width: 100%; margin-top: 15px;">Salvar Alterações</button>
        </form>
    </div>
</div>
</body>
</html>